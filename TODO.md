# Auth MockAPI integration TODO

- [ ] Update AuthService to use MockAPI endpoint and implement login/register.
- [ ] Align token storage key used by AuthService and AuthCheckView.
- [ ] Implement real login/signup logic in controllers (login -> if no account then signup).
- [ ] Ensure navigation to /home happens after successful login/signup.
- [ ] Run `flutter pub get` and verify the app compiles.

