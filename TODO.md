# TODO

- [ ] Step 1: Update `lib/core/auth/auth_theme.dart` to a cleaner/ultra-modern palette (accent + text + dividers).
- [ ] Step 2: Update Splash UI colors in `lib/feature/view/screen/slasph_screen/splash_screen.dart` to match the new clean palette.
- [ ] Step 3: Update Onboarding base colors/gradients/blobs in `lib/feature/view/screen/onborading/onborading_view.dart`.
- [ ] Step 4: Update Onboarding controls/skip/next + indicator dots in `lib/feature/view/screen/onborading/widgets/onboarding_controls.dart` and `page_indicator.dart`.
- [ ] Step 5: Update Onboarding page palette in `lib/feature/view/screen/onborading/widgets/onboarding_page_style.dart` and text/shadow colors in `onboarding_page.dart`.
- [ ] Step 6: Run `flutter analyze` and `flutter test` (or `flutter run`) to ensure no errors.
